/**
 * Telegram two-way bot polling hook (Client-side Mock).
 * Polling has been migrated to the Node.js backend server (server/telegram-bot.js)
 * to run 24/7 in the background.
 */
export function useTelegramPolling() {
  // Migrated to backend. Client polling disabled to avoid 409 conflicts.
}
